<%- model_class = Principalgroupe -%>
<div class="page-header">
  <h1><%=t '.title', :default => Principalgroupe %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:nom) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @principalgroupes.each do |principalgroupe| %>
      <tr>
        <td><%= principalgroupe.id %></td>
        <td><%= principalgroupe.nom %></td>
        <td><%=l principalgroupe.created_at, format: :long %></td>
        <td>
          <%= link_to principalgroupe_path(principalgroupe), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to principalgroupe_path(principalgroupe), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_principalgroupe_path,
            :class => 'btn btn-primary' %>
