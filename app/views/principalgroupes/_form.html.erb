<%= form_for(@principalgroupe) do |f| %>
  <% if @principalgroupe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@principalgroupe.errors.count, "error") %> prohibited this principalgroupe from being saved:</h2>

      <ul>
      <% @principalgroupe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
