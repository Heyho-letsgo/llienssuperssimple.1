<%= form_for(@sousgroupe) do |f| %>
  <% if @sousgroupe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sousgroupe.errors.count, "error") %> prohibited this sousgroupe from being saved:</h2>

      <ul>
      <% @sousgroupe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :principalgroupe_id %><br>
    <%= f.text_field :principalgroupe_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
